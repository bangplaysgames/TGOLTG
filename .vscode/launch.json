{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Game",
			"type": "lldb",
			"preLaunchTask": "build",
			"program": "${workspaceFolder}/game.exe",
			"cwd": "${workspaceFolder}",
			"console": "integratedTerminal",
			"request": "launch"
		}
	]
}
